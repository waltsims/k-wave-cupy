[project]
name = "k-wave-cupy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "scipy>=1.14.0",
]
[optional-dependencies]
cuda = [
    "cupy>=13.6.0",
]
